# Entities (L1)

One file per entity. Slug format: `<name>.md` for people, `<org-name>.md` for orgs, `<project-name>.md` for projects.

## People

*(none yet — add as agents encounter relevant contacts)*

## Organizations

*(none yet)*

## Projects

*(none yet)*
